# فدشي — سجل الإصلاحات الأمنية وإضافة لوحة الإدارة

هذا الملف يوثّق كل ما أُصلح بعد المراجعة، وخطوات تطبيقه على مشروعك
الحقيقي على Supabase.

## ⚠️ الترتيب مهم جداً — لا تغيّر الترتيب

### الخطوة 1: نفّذ ملف SQL الجديد فوراً (الأهم والأخطر)

افتح مشروعك في [supabase.com](https://supabase.com) → **SQL Editor** → New query،
والصق **محتوى** هذا الملف بالكامل ثم اضغط Run:

```
supabase/migrations/20260906010000_007_security_and_admin_fixes.sql
```

هذا يغلق فوراً ثغرة تصعيد الصلاحيات (أي مستخدم كان يستطيع جعل نفسه
super_admin بسطر واحد في الـ Console). نفّذه حتى قبل رفع أي كود جديد.

إذا كنت تستخدم Supabase CLI محلياً بدل اللصق اليدوي:
```bash
supabase db push
```

### الخطوة 2: ثبّت الكود الجديد وشغّله محلياً للتأكد

```bash
npm install
npm run dev
```

### الخطوة 3: تحقق يدوياً أن الثغرة أُغلقت فعلاً

سجّل دخولك بحساب عادي (viewer) وجرّب في Console المتصفح (F12):
```js
await supabase.from('profiles').update({ role: 'super_admin' }).eq('id', 'ضع-هنا-معرفك')
```
يجب أن تحصل الآن على خطأ `"غير مسموح بتغيير هذه الصلاحية"` بدل نجاح الطلب.

### الخطوة 4: اجعل نفسك أول super_admin

بما أن كل مستخدم جديد يُنشأ افتراضياً بصلاحية `viewer`، ولا يمكن لأحد
ترقية نفسه بعد الآن (وهذا مقصود)، نفّذ هذا **مرة واحدة فقط** من
SQL Editor في Supabase لتصبح أنت المشرف العام الأول:

```sql
UPDATE profiles SET role = 'super_admin' WHERE username = 'ضع-اسم-مستخدمك-هنا';
```

بعدها سجّل خروج ودخول من جديد في التطبيق، وستجد أيقونة ⚙️ في صفحة
حسابك تنقلك إلى `/admin`.

## ما الذي أُصلح بالتفصيل

| # | المشكلة | الحل |
|---|---|---|
| 1 | أي مستخدم يرقّي نفسه لأدمن عبر تعديل عمود role مباشرة | Trigger `protect_privileged_profile_fields` يمنع أي تعديل على role/coins/is_verified إلا من أدمن حقيقي أو الخادم |
| 2 | لا توجد لوحة إدارة فعلياً (فقط زر ديكوري) | صفحة `/admin` كاملة: مؤشرات، إدارة مستخدمين، مراجعة محتوى، بلاغات |
| 3 | الأدمن لا يرى إلا بياناته الخاصة (RLS لا يمنحه استثناء) | سياسات RLS جديدة (`*_select_admin`, `*_update_admin`) تمنح من `is_admin() = true` رؤية وإدارة كل الجداول |
| 4 | نظام البلاغات مستورد في الكود لكن غير مفعّل | زر "إبلاغ" فعلي في كل فيديو + تبويب بلاغات في لوحة الإدارة يحذف المحتوى المخالف |
| 5 | الزوار (بدون تسجيل دخول) لا يرون أي محتوى | سياسات RLS جديدة لـ `TO anon` تسمح بقراءة الفيديوهات المعتمدة والعامة فقط |
| 6 | السعر الإجمالي للطلب يُرسَل من المتصفح بدون تحقق | Trigger `enforce_order_price` يفرض السعر الحقيقي من جدول videos عند كل طلب، بغض النظر عمّا يرسله المتصفح |
| 7 | لا آلية حظر للمستخدمين المخالفين | عمود `is_banned` + منع المحظور من النشر/التعليق/الطلب عبر RLS، وزر حظر/إلغاء حظر في لوحة الإدارة |

## حدود ما تم إصلاحه (بصراحة)

**الحظر المطبَّق هنا "ناعم" (soft ban)**: يمنع المستخدم المحظور من
الكتابة (نشر، تعليق، طلب) لكنه **لا يسجّل خروجه فوراً ولا يمنعه من
تسجيل الدخول مرة أخرى**. حظر حقيقي يمنع الدخول نفسه يتطلب استدعاء
Supabase Admin API بمفتاح `service_role` من مكان آمن (Edge Function)،
لأن هذا المفتاح لا يجب أبداً أن يصل للمتصفح. إذا احتجت هذا فعلاً
(مثلاً لمستخدم يسيء الاستخدام بشكل متكرر)، أخبرني وأبني لك Edge
Function مخصصة لذلك — هذه خطوة منفصلة عن نطاق هذا الإصلاح.

## أدوار الأدمن المتاحة الآن في اللوحة

- **admin**: يرى ويدير كل المستخدمين والمحتوى والبلاغات، يستطيع حظر/توثيق المستخدمين وقبول/رفض/حذف الفيديوهات
- **super_admin**: كل صلاحيات admin، بالإضافة لترقية مستخدمين آخرين إلى admin أو إزالة الصلاحية منهم — وهو الوحيد القادر على منح صلاحية super_admin لغيره
